grammar;

pub Bosonoga: i32 = {
    <stmts:Stmt*> => {
        let nums: Vec<i32> = stmts.into_iter().filter_map(|x| x).collect();
        nums.into_iter().sum()
    },
};

Stmt: Option<i32> = {
    AppNameStmt,
    AppNamePrefixStmt,
    DiStmt,
    NumStmt,
    DoStmt,
};

AppNamePrefixStmt: Option<i32> = {
    "BOSONOGA" => Some(1000),
};

AppNameStmt: Option<i32> = {
    "main" => {
        let main_str = "main";
        println!("INFO Bosonoga::{}", main_str);
        None
    },
};

DiStmt: Option<i32> = {
    "DI" => None,
};

DoStmt: Option<i32> = {
    "DO" => None,
};

LogiStmt: i32 = {
    "LOGI" <n:NumIniThirtyTwo> => {
        match n {
            1 => println!("LOGI-1"),
            2 => println!("LOGI-2"),
            _ => println!("__LOGI__"),
        }
        0
    }
};

NumStmt: Option<i32> = {
    "DI" <n:NumIniThirtyTwo> "DO" => Some(n),
};

NumIniThirtyTwoAtom: i32 = {
    <s:r"[0-9]+"> => s.parse::<i32>().unwrap(),
};

NumIniThirtyTwo: i32 = {
    <ns:NumIniThirtyTwoAtom+> => ns.into_iter().sum(),
};
